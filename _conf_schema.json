{
  "schema_version": {
    "type": "string",
    "description": "配置模板版本",
    "default": "0.3.0",
    "invisible": true
  },
  "fallback_policy_settings": {
    "type": "object",
    "description": "兜底策略与默认项配置",
    "hint": "将用于定义插件的系统级兜底策略与默认行为。WebUI 中可创建、编辑并启用自定义策略；后续当当前策略不存在、被禁用、校验失败或无法编译时，插件将自动回退到本组配置继续执行。这里的部分设置也将为自定义策略中未显式指定的通用行为提供默认值。",
    "items": {
      "max_text_chars": {
        "type": "int",
        "description": "最大检查字符数",
        "hint": "超出部分会被截断后再检查，避免超长输入拖慢审核。此设置为所有自定义策略提供默认值。",
        "default": 6000
      },
      "max_retries": {
        "type": "int",
        "description": "最大重试次数",
        "hint": "仅作为 Step 5 的 retry_generation 系统默认值；0 表示命中后直接阻断。自定义策略可在 Step 5 设置中覆盖。",
        "default": 0
      },
      "default_llm_provider": {
        "type": "string",
        "description": "默认辅助 Provider",
        "_special": "select_provider",
        "default": "",
        "hint": "仅供本插件 LLM 旁审类规则使用。留空则相关辅助调用尝试使用 AstrBot 当前会话默认 Provider。此设置为所有自定义策略提供默认值。"
      },
      "enable_llm_review_in_fallback_policy": {
        "type": "bool",
        "description": "在兜底策略下启用 LLM 旁审。",
        "hint": "在兜底策略下启用的检测器被触发时调用 LLM 辅助分析。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": false
      },
      "default_action_on_hit": {
        "type": "string",
        "description": "默认风险动作",
        "hint": "observe=只记录；block=阻断请求。此设置为所有自定义策略提供默认值。",
        "options": ["observe", "block"],
        "default": "block"
      },
      "default_action_on_error": {
        "type": "string",
        "description": "默认错误动作",
        "hint": "discard=错误时丢弃本次规则结果，仅记录 warning，不进入依赖判断；record=错误时记录可依赖的错误结果；block=错误时阻断本轮请求或输出。此设置为所有自定义策略提供默认值。",
        "options": ["discard", "record", "block"],
        "default": "discard"
      },
      "reply_placeholder_on_block": {
        "type": "bool",
        "description": "拦截后发送提示",
        "hint": "关闭后，输入或输出被阻断时将尽量静默处理。此设置为所有自定义策略提供默认值。",
        "default": true
      },
      "block_message": {
        "type": "string",
        "description": "请求阻断提示",
        "hint": "请求被 Step 阻断时返回给用户的提示。支持 ${user_id} 和 ${step_number}；留空则使用内置默认提示。此设置为所有自定义策略提供默认值。",
        "default": "用户 ${user_id} 的请求在 Step ${step_number} 被阻断。"
      },
      "enable_encoded_payload_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输入检测器 - 编码/混淆载荷检测",
        "hint": "检测用户输入中疑似通过编码、转义或隐藏字符伪装的指令内容。用于捕捉 Base64、URL 编码、Unicode/Hex 转义、ROT13、零宽字符、方向控制符等可疑载荷。此检查只能识别明显混淆形态，复杂语义仍建议配合知识库证据裁判或 LLM 审查。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_length_anomaly_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输入检测器 - 长文本/异常结构检测",
        "hint": "检测用户输入中异常长文本、重复片段、多代码块或可疑结构堆叠。用于捕捉超长输入、大量重复字符、异常分隔符、多重代码块、隐藏文本比例异常等情况。它不判断内容是否违规，只作为异常结构节流器。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_role_marker_spoofing_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输入检测器 - 伪造角色/工具结构检测",
        "hint": "检测用户输入中疑似伪造角色或调用结构的内容。用于捕捉 role: system、developer message、function_call、tool_use、伪造日志片段、Prompt 区块等结构化伪装。它关注格式和结构，不直接判断复杂语义意图。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_external_fetch_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输入检测器 - 外部拉取/外泄检测",
        "hint": "检测用户是否诱导模型读取外部链接、下载提示词或通过外链传递隐藏指令。用于捕捉读取 URL、load prompt from、fetch/download、Markdown 图片外泄、curl/wget/powershell 等外链相关风险。普通链接不一定命中，通常需要与拉取/执行意图共现。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_instruction_override_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输入检测器 - 越权与指令覆盖检测",
        "hint": "检测用户是否试图覆盖系统指令、索要隐藏提示词或绕过既有约束。用于捕捉忽略前文、进入开发者模式、泄露系统提示词、突破安全限制、覆盖人格设定等明显越权请求。领域或人格相关边界建议配合知识库证据裁判。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_multi_turn_escalation_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输入检测器 - 多轮渐进诱导检测",
        "hint": "检测同一会话中风险信号是否在多轮对话内持续累积或升级。用于捕捉短时间内多次试探、逐步诱导、反复索要隐藏规则等渐进式风险。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_prompt_injection_combo_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输入检测器 - 注入组合信号检测",
        "hint": "综合当前输入中的多个弱风险信号，判断是否构成明显提示词注入风险。此检查只组合当前输入内的本地信号，例如越权词、系统提示词引用、角色标记、长文本或代码块共现。它不引用其他规则结果，复杂组合请使用逻辑门。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_format_violation_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输出检测器 - 输出格式违规检测",
        "hint": "检测模型回复是否违反预期输出格式。适用于要求 JSON、YAML、纯文本、单行回复、固定字段或禁止 Markdown 的场景。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_poor_quality_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输出检测器 - 异常低质回复检测",
        "hint": "检测空回复、过短回复、重复内容、乱码或模板化异常回复。用于捕捉明显的生成异常，例如只有标点、重复字符、重复句子、空白回复、模型报错模板等。它不评价内容好坏，只处理明显异常。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_metadata_leakage_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输出检测器 - 元信息泄漏检测",
        "hint": "检测回复中是否泄漏工具调用、调试信息、Provider 信息或内部字段。用于捕捉 tool_call、function_call、traceback、debug、provider_id、session_id、raw_completion、internal marker 等不应出现在最终回复中的元信息。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_sensitive_echo_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输出检测器 - 风险输入复述检测",
        "hint": "检测模型是否原样复述了用户输入中的高风险片段或敏感内容。会结合输入分析阶段记录的命中片段、净化片段或危险关键词，判断输出是否再次暴露这些内容。适合拦截提示词注入内容、危险指令或敏感文本的回显。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_language_drift_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输出检测器 - 回复语言漂移检测",
        "hint": "检测模型回复是否明显偏离预期语言。用于粗略判断回复语言是否偏离配置要求，例如要求中文却大段输出英文。它只做本地节流判断，复杂语境建议配合 LLM 审查。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      },
      "enable_prompt_leakage_detector": {
        "invisible": true,
        "type": "bool",
        "description": "启用输出检测器 - 内部提示泄漏检测",
        "hint": "检测模型回复中疑似泄漏系统提示词、开发者指令或隐藏规则的内容。用于捕捉明显的内部提示泄漏迹象，例如 system prompt、developer instructions、隐藏规则、BEGIN SYSTEM PROMPT 等。复杂语义判断建议配合知识库证据裁判或 LLM 审查。此开关仅用于控制该功能在兜底策略下的激活状态。",
        "default": true
      }
    }
  },
  "system_constants": {
    "type": "object",
    "description": "系统常量",
    "hint": "所有策略与系统兜底图共享的静态文本表。常量不是规则或元件，不进入 Canvas、调度、依赖、逻辑门或 NodeSignal。名称只能使用 1 至 64 个大写字母、数字和下划线；在策略文本模板中以 ${常量名} 引用，如果命名格式错误则无法被解析为可用常量。",
    "items": {},
    "default": {}
  },
  "session_control": {
    "type": "object",
    "description": "生效会话控制",
    "hint": "用于决定哪些会话会进入 LLM Guardrail 流程。未纳入生效范围的会话可以选择放行或拦截。可在目标会话发送 /guardrail 查看当前 UMO。",
    "items": {
      "group_chat_mode": {
        "type": "string",
        "description": "群聊处理范围",
        "hint": "all_run=所有群聊都进入 Guardrail rails；all_pass=所有群聊都跳过 Guardrail 并放行；all_block=所有群聊都跳过 Guardrail 并拦截；enabled_or_pass=只有指定群聊进入 rails，其他群聊跳过并放行；enabled_or_block=只有指定群聊进入 rails，其他群聊跳过并拦截。",
        "options": [
          "all_run",
          "all_pass",
          "all_block",
          "enabled_or_pass",
          "enabled_or_block"
        ],
        "default": "all_run"
      },
      "group_chat_enabled": {
        "type": "list",
        "description": "指定群聊 UMO",
        "hint": "当群聊处理范围为 enabled_or_pass 或 enabled_or_block 时，只有这里列出的群聊会进入 Guardrail。这里不是豁免列表。",
        "items": {
          "type": "string"
        },
        "default": []
      },
      "private_chat_mode": {
        "type": "string",
        "description": "私聊处理范围",
        "hint": "all_run=所有私聊都进入 Guardrail rails；all_pass=所有私聊都跳过 Guardrail 并放行；all_block=所有私聊都跳过 Guardrail 并拦截；enabled_or_pass=只有指定私聊进入 rails，其他私聊跳过并放行；enabled_or_block=只有指定私聊进入 rails，其他私聊跳过并拦截。",
        "options": [
          "all_run",
          "all_pass",
          "all_block",
          "enabled_or_pass",
          "enabled_or_block"
        ],
        "default": "all_run"
      },
      "private_chat_enabled": {
        "type": "list",
        "description": "指定私聊 UMO",
        "hint": "当私聊处理范围为 enabled_or_pass 或 enabled_or_block 时，只有这里列出的私聊会进入 Guardrail。这里不是豁免列表。",
        "items": {
          "type": "string"
        },
        "default": []
      }
    }
  },
  "access_control": {
    "type": "object",
    "description": "输入访问控制",
    "hint": "管理输入阶段的临时封禁。完整管理建议在插件面板中进行。",
    "items": {
      "auto_blacklist_enabled": {
        "type": "bool",
        "description": "启用自动拉黑",
        "default": false
      },
      "blacklist_duration_minutes": {
        "type": "int",
        "description": "封禁时长（分钟）",
        "hint": "-1 表示永久；仅允许 -1 或正整数。",
        "default": 60
      },
      "blacklist_max_violations": {
        "type": "int",
        "description": "触发封禁的违规次数",
        "default": 3
      },
      "blacklist_message": {
        "type": "text",
        "description": "黑名单提示文本",
        "hint": "支持 ${user_id}。留空仍使用内置默认提示；静默请将提示间隔设为 -1。",
        "default": "用户 ${user_id} 已因多次触发风险规则被临时限制使用，请稍后再试。"
      },
      "blacklist_message_interval_minutes": {
        "type": "int",
        "description": "提示间隔（分钟）",
        "hint": "同一已封禁用户在此间隔内只提示一次。0 表示每次都提示；-1 表示静默；其他负数无效。",
        "default": 5
      }
    }
  },
  "session_policy_state": {
    "type": "object",
    "description": "会话策略状态监控",
    "hint": "P2-A 记录每个 UMO 最近的策略执行结果、路由候选/请求目标观察和活动记录，并在会话策略监控面板中只读展示。它不会改变现有策略执行或固定 Provider。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用 UMO 状态监控",
        "hint": "关闭后不再记录或刷新 UMO 状态；不会影响 Guardrail 策略本身的执行。",
        "default": true
      },
      "state_ttl_seconds": {
        "type": "int",
        "description": "UMO 非活动保留时长（秒）",
        "hint": "UMO 在此时长内没有新的状态活动时删除其完整监控记录。设置为 0 表示不按时间删除，但仍受最大状态数量限制。最小非零值为 60 秒。",
        "default": 604800
      },
      "max_entries": {
        "type": "int",
        "description": "最大 UMO 状态数量",
        "hint": "超过上限时，优先淘汰最久未更新的 UMO 完整状态记录。必须为正整数。",
        "default": 500
      },
      "activity_log_limit": {
        "type": "int",
        "description": "每个 UMO 的活动记录上限",
        "hint": "每个 UMO 仅保留最近 N 条活动记录；超过后按时间顺序淘汰最早记录。必须为正整数。",
        "default": 50
      }
    }
  },
  "debug_settings": {
    "type": "object",
    "description": "调试设置",
    "items": {
      "enable_stats": {
        "type": "bool",
        "description": "启用统计",
        "default": true
      },
      "stats_max_records": {
        "type": "int",
        "description": "最近记录上限",
        "default": 200
      },
      "logging": {
        "type": "bool",
        "description": "调试日志",
        "hint": "开启后会记录本轮请求和响应的摘要，方便检查配置是否命中。",
        "default": false
      }
    }
  }
}
